name: Visual Smoke

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Build production app
        run: npm run build

      - name: Render desktop and mobile smoke screenshots
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p visual-smoke

          cat > dist/__visual-seed.html <<'HTML'
          <!doctype html>
          <meta charset="utf-8" />
          <title>DayDock visual fixture</title>
          <script>
            const mode = new URLSearchParams(location.search).get("mode") ?? "mature";
            const now = new Date();
            const iso = (offsetMinutes = 0) =>
              new Date(now.getTime() + offsetMinutes * 60_000).toISOString();
            const dateKey = now.toISOString().slice(0, 10);

            const tasks = {
              release: {
                id: "release",
                title: "Ship DayDock release",
                status: "today",
                estimateMinutes: 50,
                personId: null,
                createdAt: iso(-180),
                completedAt: null,
              },
              qa: {
                id: "qa",
                title: "Verify mobile release flow",
                status: "today",
                estimateMinutes: 25,
                personId: null,
                createdAt: iso(-150),
                completedAt: null,
              },
              followup: {
                id: "followup",
                title: "Ask Anna for final copy review",
                status: "inbox",
                estimateMinutes: null,
                personId: "anna",
                createdAt: iso(-120),
                completedAt: null,
              },
              later: {
                id: "later",
                title: "Explore richer install screenshots",
                status: "later",
                estimateMinutes: null,
                personId: null,
                createdAt: iso(-90),
                completedAt: null,
              },
              done: {
                id: "done",
                title: "Review release checklist",
                status: "done",
                estimateMinutes: 20,
                personId: null,
                createdAt: iso(-240),
                completedAt: iso(-60),
              },
            };

            const state = {
              tasks,
              taskOrder: ["release", "qa", "followup", "later", "done"],
              top3: ["release", "qa"],
              people: {
                anna: {
                  id: "anna",
                  name: "Anna",
                  context: "Final release copy and mobile feedback",
                  nextFollowUpDate: dateKey,
                  createdAt: iso(-1440),
                },
              },
              personOrder: ["anna"],
              focus: {
                active:
                  mode === "focus"
                    ? {
                        id: "focus-release",
                        taskId: "release",
                        startedAt: iso(-8),
                        durationMinutes: 50,
                        pausedAt: null,
                        accumulatedPauseMs: 0,
                      }
                    : null,
                history: [
                  {
                    id: "focus-done",
                    taskId: "done",
                    startedAt: iso(-95),
                    durationMinutes: 20,
                    pausedAt: null,
                    accumulatedPauseMs: 0,
                    endedAt: iso(-75),
                    outcome: "completed",
                  },
                ],
              },
            };

            localStorage.setItem(
              "daydock:workspace",
              JSON.stringify({
                schemaVersion: 2,
                updatedAt: iso(),
                data: state,
              }),
            );
            location.replace("/");
          </script>
          HTML

          npm run preview -- --host 127.0.0.1 --port 4173 > visual-smoke/preview.log 2>&1 &
          preview_pid=$!
          trap 'kill "${preview_pid}" 2>/dev/null || true' EXIT

          ready=false
          for attempt in {1..30}; do
            if curl -fsS http://127.0.0.1:4173/ > /dev/null; then
              ready=true
              break
            fi
            sleep 1
          done

          if [ "${ready}" != "true" ]; then
            cat visual-smoke/preview.log
            echo "::error::Vite preview did not become ready."
            exit 1
          fi

          browser="$(
            command -v google-chrome ||
            command -v google-chrome-stable ||
            command -v chromium ||
            command -v chromium-browser ||
            true
          )"

          if [ -z "${browser}" ]; then
            echo "::error::No Chromium-compatible browser is available on the runner."
            exit 1
          fi

          common_flags=(
            --headless=new
            --no-sandbox
            --disable-dev-shm-usage
            --hide-scrollbars
            --run-all-compositor-stages-before-draw
            --virtual-time-budget=2500
          )

          dump_dom() {
            local profile="$1"
            local url="$2"
            local output="$3"
            local status=0

            set +e
            timeout 20s "${browser}" "${common_flags[@]}" \
              --user-data-dir="${profile}" \
              --dump-dom \
              "${url}" > "${output}"
            status=$?
            set -e

            if [ "${status}" -ne 0 ] && [ "${status}" -ne 124 ]; then
              echo "::error::Chromium DOM render failed for ${url} with status ${status}."
              return "${status}"
            fi

            if [ ! -s "${output}" ]; then
              echo "::error::Chromium produced no DOM for ${url}."
              return 1
            fi

            if [ "${status}" -eq 124 ]; then
              echo "::notice::Chromium stayed alive after producing DOM for ${url}; accepting the captured document and continuing assertions."
            fi
          }

          timeout 20s "${browser}" "${common_flags[@]}" \
            --user-data-dir=/tmp/daydock-desktop \
            --window-size=1440,1100 \
            --screenshot="$GITHUB_WORKSPACE/visual-smoke/desktop.png" \
            http://127.0.0.1:4173/

          timeout 20s "${browser}" "${common_flags[@]}" \
            --user-data-dir=/tmp/daydock-mobile \
            --window-size=390,844 \
            --screenshot="$GITHUB_WORKSPACE/visual-smoke/mobile.png" \
            http://127.0.0.1:4173/

          timeout 20s "${browser}" "${common_flags[@]}" \
            --user-data-dir=/tmp/daydock-small-mobile \
            --window-size=360,740 \
            --screenshot="$GITHUB_WORKSPACE/visual-smoke/small-mobile.png" \
            http://127.0.0.1:4173/

          dump_dom \
            /tmp/daydock-dom \
            http://127.0.0.1:4173/ \
            visual-smoke/dom.html

          timeout 20s "${browser}" "${common_flags[@]}" \
            --user-data-dir=/tmp/daydock-mature-desktop \
            --window-size=1440,1100 \
            --screenshot="$GITHUB_WORKSPACE/visual-smoke/mature-desktop.png" \
            "http://127.0.0.1:4173/__visual-seed.html?mode=mature"

          timeout 20s "${browser}" "${common_flags[@]}" \
            --user-data-dir=/tmp/daydock-mature-mobile \
            --window-size=390,844 \
            --screenshot="$GITHUB_WORKSPACE/visual-smoke/mature-mobile.png" \
            "http://127.0.0.1:4173/__visual-seed.html?mode=mature"

          timeout 20s "${browser}" "${common_flags[@]}" \
            --user-data-dir=/tmp/daydock-mature-small-mobile \
            --window-size=360,740 \
            --screenshot="$GITHUB_WORKSPACE/visual-smoke/mature-small-mobile.png" \
            "http://127.0.0.1:4173/__visual-seed.html?mode=mature"

          timeout 20s "${browser}" "${common_flags[@]}" \
            --user-data-dir=/tmp/daydock-focus-desktop \
            --window-size=1440,1100 \
            --screenshot="$GITHUB_WORKSPACE/visual-smoke/focus-desktop.png" \
            "http://127.0.0.1:4173/__visual-seed.html?mode=focus"

          timeout 20s "${browser}" "${common_flags[@]}" \
            --user-data-dir=/tmp/daydock-focus-mobile \
            --window-size=390,844 \
            --screenshot="$GITHUB_WORKSPACE/visual-smoke/focus-mobile.png" \
            "http://127.0.0.1:4173/__visual-seed.html?mode=focus"

          dump_dom \
            /tmp/daydock-mature-dom \
            "http://127.0.0.1:4173/__visual-seed.html?mode=mature" \
            visual-smoke/mature-dom.html

          dump_dom \
            /tmp/daydock-focus-dom \
            "http://127.0.0.1:4173/__visual-seed.html?mode=focus" \
            visual-smoke/focus-dom.html

          grep -q "DayDock" visual-smoke/dom.html
          grep -q "Capture your first item" visual-smoke/dom.html
          grep -q "DayDock getting started" visual-smoke/dom.html
          grep -q "Ship DayDock release" visual-smoke/mature-dom.html
          grep -q "Top 3" visual-smoke/mature-dom.html
          grep -q "Ship DayDock release" visual-smoke/focus-dom.html
          grep -q "End session" visual-smoke/focus-dom.html

          test -s visual-smoke/desktop.png
          test -s visual-smoke/mobile.png
          test -s visual-smoke/small-mobile.png
          test -s visual-smoke/mature-desktop.png
          test -s visual-smoke/mature-mobile.png
          test -s visual-smoke/mature-small-mobile.png
          test -s visual-smoke/focus-desktop.png
          test -s visual-smoke/focus-mobile.png

      - name: Upload visual smoke evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: daydock-visual-smoke
          path: visual-smoke/
          if-no-files-found: warn
          retention-days: 14
